# 操作方法

## 工具准备

### AutoHotKey
1. 下载地址：https://www.autohotkey.com/
2. 脚本：
   ```
    #Requires AutoHotkey v2.0

    ; Force the script to run as Administrator
    if !A_IsAdmin
    {
    try Run('*RunAs "' A_ScriptFullPath '"')
    ExitApp()
    }

    ; Press F8 to move time forward by 1 hour
    F8::
    {
    ; We use single quotes for the PowerShell string to avoid syntax errors
    RunWait('powershell.exe -Command "Set-Date (Get-Date).AddHours(1)"', , "Hide")

    ; Visual feedback near your mouse cursor
    ToolTip("Time +1 Hour")
    SetTimer(() => ToolTip(), -2000) 
    }

    ; Press Ctrl + F8 to close this script entirely
    ^F8::ExitApp()
   ```

### profile.yaml

- blackmarket_items
  - blackmarket_itemtype: BOR_SR → 物品类型
  - blackmarket_itemcomp: bor_sr.comp_05_legendary_Vamoose → 物品名称

### 1.yaml

- blackmarket_cooldown: 1758054660
- last_played_timestamp: 1774746333
- 最后将存档里的 blackmarket_cooldown < last_played_timestamp